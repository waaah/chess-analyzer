export enum PlayerColor {
    WHITE = "white",
    BLACK = "black",
}

export enum GameModes {
    BLITZ = "blitz",
    RAPID = "rapid",
    BULLET = "bullet",
}

export enum ChessGameSource {
    CHESS_COM = "chess.com",
    LICHESS = "lichess",
}